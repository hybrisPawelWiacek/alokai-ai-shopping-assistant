import { Inter } from 'next/font/google';

export const fontBody = Inter({
  subsets: ['latin'],
  variable: '--font-body',
});

export const fontHeadings = Inter({
  subsets: ['latin'],
  variable: '--font-headings',
});
